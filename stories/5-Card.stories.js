import React, {useState} from 'react'
import {Card} from "../src/components/card"

export default {
  title: 'Card',
}

export const Base = () => (
	<Card titleContent={'Metallica'}>
		test
	</Card>
)